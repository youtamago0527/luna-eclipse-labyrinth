# Claude担当範囲：月蝕タイムアタック・ミッション

このリポジトリは「ルナと月蝕の迷宮」のWeb／アプリ用ゲームです。

## 担当するもの

既存画面とは独立して、以下を実装してください。

1. `mission.html` — スマホ用ミッション確認画面の単独プレビュー
2. `mission.css` — ミッション画面専用スタイル（セーフエリア必須）
3. `js/missions/mission-config.js` — 解放条件・発生率・報酬階の設定
4. `js/missions/mission-system.js` — 純粋ロジック
   - B20F到達による解放判定
   - 初期25%、強化1段階につき+1%、最大40%の発生率計算
   - 帰還後の次回潜行に対する抽選
   - 発生中の再抽選防止
   - 制限時間内の最高到達階記録
   - 到達階以下の途中報酬一覧と最高アイテムランクの算出
5. `js/missions/mission-preview.js` — `mission.html`だけで挙動確認するための接続処理
6. `tests/mission-system.test.mjs` — 上記純粋ロジックのテスト

## 確定仕様

- B20F初到達で機能解放。
- 解放時に恒久遺物「月蝕の砂時計」を獲得。
- 解放前は「B20F到達で解放」と表示。
- 解放後、帰還して次の潜行へ進む際に抽選。
- 「月蝕の砂時計」Lv.1の発生率は25%。
- 強化1段階につき+1%、上限40%。
- 発生中は再抽選しない。
- 制限時間内の到達階が高いほど恒久アイテムのランクが上がる。
- 到達済みの途中階報酬もすべて獲得。
- 具体的な制限時間、階層別報酬、ランク区分は調整可能な設定値とし、仮値で明示する。

## UI要件

- 拠点と同じ黒紺・青い月光・控えめな金装飾。
- 最大430×932のスマホ縦画面。
- `viewport-fit=cover` と上下左右の `env(safe-area-inset-*)` を使用。
- 解放前、未発生、発生中、結果確定の4状態を単独プレビューで切り替え可能にする。
- ミッション内容、残り時間、階層別報酬、現在到達階、次の報酬階を表示。
- 外部フレームワーク、CDN、課金、広告、ランキング、通信、保存機能は追加しない。

## 編集禁止

競合を避けるため、以下は変更しないでください。

- `index.html`
- `hub.html`
- `dungeon.html`
- `start.css`
- `styles.css`
- `hub-layout.css`
- `dungeon.css`
- `main.js`
- `js/dungeon/**`
- `assets/**`
- `docs/GAME_DESIGN.md`

既存ファイルとの接続はCodex側で行います。必要な接続内容は `docs/claude/INTEGRATION_NOTES.md` に記録してください。

## 完了方法

- ブランチ `feature/claude-mission-ui` 内だけで作業する。
- テストを実行し、結果を `docs/claude/INTEGRATION_NOTES.md` に記録する。
- 作業完了時にすべてコミットし、最終コミットIDを報告する。
- `main`へのマージ、push、公開は行わない。
